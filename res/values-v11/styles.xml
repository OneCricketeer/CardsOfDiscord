<resources>

    <!--
        Base application theme for API 11+. This theme completely replaces
        AppBaseTheme from res/values/styles.xml on API 11+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Black">
        <!-- API 11 theme customizations can go here. -->
    </style>

    <style name="Theme.Default" parent="@android:style/Theme.Black"></style>

    <style name="Theme.NoTitle" parent="@android:style/Theme.Black.NoTitleBar"></style>

    <style name="Theme.FullScreen" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen"></style>

</resources>